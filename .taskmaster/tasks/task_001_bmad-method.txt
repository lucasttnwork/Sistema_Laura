# Task ID: 1
# Title: Setup Project Infrastructure
# Status: done
# Dependencies: None
# Priority: high
# Description: Initialize the project repository with the specified tech stack including React with TypeScript, Express.js with TypeScript, PostgreSQL via Docker, and Prisma ORM. Configure Docker for local development environment.
# Details:
Create a new repository. Set up package.json for both frontend (React 18+, Next.js, Tailwind CSS, React Hook Form, React Query) and backend (Node.js 18+, Express.js, Prisma, Redis). Configure Docker Compose for PostgreSQL and Redis. Install dependencies and set up basic folder structure following best practices. Use Prisma to initialize schema.

# Test Strategy:
Verify Docker containers start successfully, dependencies install without errors, and basic server/frontend can run locally.

# Subtasks:
## 1. Initialize Repository and Basic Structure [done]
### Dependencies: None
### Description: Create a new Git repository and establish the root folder structure following industry best practices for full-stack TypeScript projects.
### Details:
Set up root directories: /frontend, /backend, /prisma, /docker. Initialize Git and create initial README with project overview.

## 2. Configure Frontend with React, Next.js, and Dependencies [done]
### Dependencies: 1.1
### Description: Set up the frontend package.json and install React 18+, Next.js, TypeScript, Tailwind CSS, React Hook Form, and React Query.
### Details:
Use create-next-app with TypeScript template. Install and configure Tailwind, React Hook Form, and React Query. Set up tsconfig and basic layout components.

## 3. Configure Backend with Express.js, Prisma, and Redis [done]
### Dependencies: 1.1
### Description: Set up backend package.json and install Node.js 18+, Express.js, TypeScript, Prisma, Redis client, and supporting libraries.
### Details:
Initialize Express app with TypeScript. Install Prisma ORM and Redis client. Configure tsconfig, ESLint, and basic server structure with health check endpoint.

## 4. Configure Docker Compose for PostgreSQL and Redis [done]
### Dependencies: 1.1
### Description: Create Docker Compose file to run PostgreSQL and Redis containers for local development.
### Details:
Define services for PostgreSQL (with persistent volume) and Redis in docker-compose.yml. Expose appropriate ports and set environment variables.

## 5. Initialize Prisma Schema and Connect to Database [done]
### Dependencies: 1.3, 1.4
### Description: Initialize Prisma ORM, define initial schema.prisma, and run migration to create tables in PostgreSQL.
### Details:
Run prisma init. Define initial User and Work models in schema.prisma. Generate Prisma Client and run migration against Dockerized PostgreSQL.

